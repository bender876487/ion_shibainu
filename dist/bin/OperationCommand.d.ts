/**
 * Class that handles the `operation` CLI command.
 */
export default class OperationCommand {
    /**
     * Handles the `create` sub-command.
     */
    static handleCreate(): Promise<void>;
}
